
기본적으로 설정된 기준점과 일반적인 기기 크기는 다음과 같습니다.

- **(기본)** `(접두사 없음)`: 모바일 화면
    
- **`sm`**: `640px` 이상 (세로 모드 태블릿 정도)
    
- **`md`**: `768px` 이상 (가로 모드 태블릿 정도)
    
- **`lg`**: `1024px` 이상 (노트북, 작은 데스크톱)
    
- **`xl`**: `1280px` 이상 (일반 데스크톱)
    
- **`2xl`**: `1536px` 이상 (큰 데스크톱)